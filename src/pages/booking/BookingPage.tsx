import { PageShell } from "../shared/PageShell";

export function BookingPage() {
  return (
    <PageShell
      title="Booking"
      description="A dedicated booking page prepared for calendar selection, forms and private session requests."
    />
  );
}
