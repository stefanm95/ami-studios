import { PageShell } from "../shared/PageShell";

export function ContactPage() {
  return (
    <PageShell
      title="Contact"
      description="A dedicated contact page prepared for phone, WhatsApp, location and concierge requests."
    />
  );
}
